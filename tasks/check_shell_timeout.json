{
  "description": "Check default user shell timeout is 900 seconds or less (configurable via the tmout parameter).",
  "input_method": "environment",
  "parameters": {
    "tmout": {
      "description": "Maximal timeout setting.",
      "type": "Integer",
      "default": "900"
    }
  }
}
