---
cis_security_hardening::rules::sshd_install::enforce: false  # Service['sshd'] fails: unit is ssh.service here
cis_security_hardening::rules::log_suspicious_packets::enforce: false  # live sysctl reads back changed on 2nd apply
cis_security_hardening::rules::mta_local::enforce: false  # aide pulls in postfix mid-run, so the postfix fact only flips on the next run
